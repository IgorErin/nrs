[package]
name = "nfs-mamont"
version = "0.0.0"
edition = "2021"
authors = ["Igor Erin"]
description = "A Rust NFS Server implementation"
publish = false
rust-version = "1.90.0"

[lib]
doctest = false

[dependencies]
tokio = { version = "1.49", features = ["full", "time"] }
